# 系统重组验证记录

验证日期：2026-08-04

## 构建结果

六个工作空间按照固定 overlay 顺序全部构建成功：

| 工作空间 | 包数量 | 结果 |
|---|---:|---|
| camera_ws | 4 | 通过 |
| chassis_ws | 7 | 通过 |
| aoa_ws | 2 | 通过 |
| rtk_ws | 3 | 通过 |
| perception_ws | 3 | 通过 |
| navigation_ws | 4 | 通过 |

合计 23 个 ROS 2 包。

构建脚本强制使用 `/usr/bin/python3`，已消除 Conda base 环境导致的 `catkin_pkg` 缺失问题。

## 单元测试

- AOA 坐标转换与鲁棒滤波：10 项通过。
- UM982/NTRIP 与 RTK 地图定位：16 项通过。
- 正射地图和占据格栅：6 项通过。
- 感知现有 lint：2 项通过，1 项版权检查跳过。
- 串口桥接 lint：2 项通过，1 项版权检查跳过。

Orbbec 上游 `orbbec_camera_msgs` 有既有 CMake 空格 lint 问题；其 XML lint 还会联网读取 ROS schema，当前网络返回空内容。上游包已经成功构建，这两项不作为本系统源码失败处理，也没有修改第三方代码来规避测试。

## Launch 解析

以下入口均已通过 `ros2 launch ... --show-args` 参数解析：

- `agri_camera_bringup/gemini335l.launch.py`
- `agri_global_localization/aoa_global_localization.launch.py`
- `agri_rtk_localization/rtk_system.launch.py`
- `agri_robot_bringup/sim_bringup.launch.py`
- `agri_vslam_bringup/rgbd_vslam_camera_only_stable_scan.launch.py`
- `agri_map_integration/integrated_mapping.launch.py`
- `agri_nav2_config/nav2_minimal_formal.launch.py`

所有包前缀均指向 `/home/czb/agri_robot_system`，没有解析到旧工作空间。

## 尚未执行

- 未自动占用 AOA、UM982、Orbbec 或底盘串口设备。
- 未进行相机、AOA、RTK 的本轮硬件联合运行测试。
- 未进行无底盘条件下无法完成的 Nav2 闭环测试。
- GitHub 私有仓库未认证，尚未创建远端或推送。
