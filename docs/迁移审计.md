# 迁移审计记录

## 迁移原则

本次采用非破坏迁移：旧源码保持原位，新系统复制有效源码并独立构建。只有新系统通过构建和回归测试后，旧目录才允许进入归档；不直接删除用户代码或实验数据。

## 有效源码映射

| 原位置 | 新位置 | 处理 |
|---|---|---|
| `pythonProject01/OrbbecSDK_ROS2` | `camera_ws/src/OrbbecSDK_ROS2` | 保留独立 Git 历史和本地补丁 |
| `air_ground_cooperation_system_work/systems/aoa` | `aoa_ws/src/agri_aoa_driver` | 转换为 ament_python 包 |
| `ROS_RTK/ros2_ws/src` | `rtk_ws/src` | 驱动、接口和定位合并 |
| `trunk_tracking_ws/src` 底盘相关包 | `chassis_ws/src` | 消除顶层 Python 路径依赖 |
| `trunk_tracking_ws/src` 感知相关包 | `perception_ws/src` | RTAB-Map、树干检测与深度障碍归一 |
| `trunk_tracking_ws/src` 地图与导航包 | `navigation_ws/src` | Nav2、正射地图和集成入口归一 |

## 明确排除

以下内容不是当前系统组成，不迁入活动工作空间，也不删除原目录：

- YDLidar
- Livox
- HWT601 外置 IMU
- visual-crop-row-navigation
- 旧 FAST-LIO 与 MPPI 实验代码

## 已发现并处理的结构问题

- AOA launch 依赖绝对路径执行外部 Python 文件：已改为 ROS 2 `Node`。
- RTK 驱动和地图定位分别位于两个工作空间：已合并并增加总 launch。
- 底盘节点从 `/home/czb/pythonProject01` 导入模块：已建立共享串口包。
- 树干模型依赖主目录：已安装到 `trunk_detection` 包 share 目录。
- 相机 launch 使用源码绝对路径：新入口使用 `FindPackageShare`。
- 底盘寄存器参数重复且设备 ID 命名不一致：新工作空间已统一。

## 尚未执行的清理

- 旧目录尚未移动或删除。
- 主目录中的安装包、压缩包和其他独立项目尚未处理。
- GitHub 远端尚未创建；本机当前没有 GitHub CLI、SSH 密钥或已验证凭据。
